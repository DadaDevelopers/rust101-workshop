// SESSION 9 — Follow Along: Validation with Result
// Run with: cargo run
// We'll validate Bitcoin addresses and amounts together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn validate_amount(sats: u64) -> Result<(), &'static str> { ... }
// fn parse_address(s: &str) -> Result<String, &'static str> { ... }

// SESSION 8 — Follow Along: Structs & Ownership
// Run with: cargo run
// We'll model a UTXO struct and explore move semantics together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// struct Utxo { txid: String, vout: u32, value_sats: u64 }
// impl Utxo { fn new(...) -> Self { ... } }
// fn total_value(utxos: &[Utxo]) -> u64 { ... }

// SESSION 11 — Follow Along: Stack vs Heap
// Run with: cargo run
// We'll compare stack allocation with Box/Vec and see the difference.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn stack_utxo() -> Utxo { ... }          // returned by value (stack)
// fn heap_utxos(n: usize) -> Vec<Utxo> { ... }  // allocated on the heap

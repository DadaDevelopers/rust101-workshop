// SESSION 10 — Follow Along: Modules
// Run with: cargo run
// We'll organise wallet logic into modules together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// mod wallet { pub fn balance(utxos: &[u64]) -> u64 { ... } }
// mod fees   { pub fn estimate(size_vbytes: u64, fee_rate: u64) -> u64 { ... } }

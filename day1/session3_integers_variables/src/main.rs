// SESSION 3 — Follow Along: Integers & Variables
// Run with: cargo run
// We'll explore integer types and variable bindings together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// const BTC_TO_SATS: u64 = ...;
// fn btc_to_sats(btc: f64) -> u64 { ... }
// fn sats_to_btc(sats: u64) -> f64 { ... }

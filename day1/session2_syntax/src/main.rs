// SESSION 2 — Follow Along: Rust Syntax Basics
// Run with: cargo run
// We're going to write our first functions together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn greet(name: &str) -> String { ... }
// fn tx_label(confirmed: bool) -> &'static str { ... }

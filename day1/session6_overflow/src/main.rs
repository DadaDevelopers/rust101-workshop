// SESSION 6 — Follow Along: Integer Overflow
// Run with: cargo run
// We'll trigger overflow and learn the safe alternatives.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn total_supply(block_subsidy: u64, blocks: u64) -> Option<u64> { ... }
// fn checked_fee(amount: u64, fee: u64) -> Option<u64> { ... }

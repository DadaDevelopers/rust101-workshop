// SESSION 5 — Follow Along: Panics
// Run with: cargo run
// We'll see what panics look like and when to use them.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn get_output(outputs: &[u64], index: usize) -> u64 { ... }  // panic on bad index
// fn parse_txid(hex: &str) -> [u8; 32] { ... }                 // panic if wrong length

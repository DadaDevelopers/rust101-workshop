// SESSION 4 — Follow Along: Branching & Loops
// Run with: cargo run
// We'll build a fee-tier classifier and a block counter together.

fn main() {
    // we'll fill this in together
}

// You'll write these as Nkatha explains:
// fn fee_tier(sats_per_vbyte: u64) -> &'static str { ... }  // "low" / "medium" / "high"
// fn count_confirmations(start: u32, end: u32) -> u32 { ... }
